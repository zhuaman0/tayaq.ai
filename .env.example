# OpenAI — used for /chat (text), /api/stt (Whisper), /api/tts
OPENAI_API_KEY=sk-...

# Google Gemini — used for /live (Realtime voice)
# Get one at https://aistudio.google.com/apikey
GEMINI_API_KEY=

# Supabase — auth, profiles, vocab, leaderboards
SUPABASE_URL=
SUPABASE_KEY=
SUPABASE_SERVICE_KEY=

# Supadata — YouTube transcript fetching (works on Vercel where direct YouTube
# scraping is blocked). Free tier 100 transcripts/month at https://supadata.ai
SUPADATA_API_KEY=
